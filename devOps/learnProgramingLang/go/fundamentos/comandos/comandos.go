package main

import "fmt"

func main() {
	fmt.Printf("Outro programa em %s!", "Go")
}

// cli
// go run FILE -> roda os arquivos
// go build FILE -> compila os arquivos
// go get DEPENDENCES -> baixa os pacotes
// go version -> mostra a versão do go
// go env -> mostra as variáveis de ambiente
// go doc -http=:6060 -> mostra a documentação do go
// go doc cmd/vet -> mostra a documentação do vet
